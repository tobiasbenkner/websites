{
  "name": "@tobiasbenkner/websites",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "docker:build": "turbo run docker:build",
    "docker:push": "turbo run docker:push",
    "check-types": "turbo run check-types",
    "update-deps": "npm-check-updates -u --deep"
  },
  "devDependencies": {
    "turbo": "^2.5.5"
  },
  "packageManager": "pnpm@10.25.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "esbuild",
      "unrs-resolver",
      "msgpackr-extract"
    ]
  }
}